type Meeting
    @model
    @auth(
        rules: [{ allow: owner, ownerField: "users", identityClaim: "email" }]
    ) {
    uid: ID! @primaryKey
    name: String!
    id: String!
    platform: String!
    password: String
    time: AWSTimestamp!
    status: String
    users: [String]
}
