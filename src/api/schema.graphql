type Meeting @model @auth(rules: [{ allow: owner, ownerField: "users" }]) {
    uid: ID! @primaryKey
    name: String!
    id: String!
    platform: String!
    password: String
    time: AWSTimestamp!
    scribe: String
    status: String
    users: [String]
}
